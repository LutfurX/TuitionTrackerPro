import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tuition.tracker.app',
  appName: 'Tuition Tracker Pro',
  webDir: 'dist'
};

export default config;
